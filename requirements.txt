streamlit>=1.20.0
PyPDF2>=3.0.0
openai>=0.27.0
tiktoken>=0.4.0
# Pin langchain to the latest 0.1.x that is compatible with langchain-community <0.1
# to avoid the 0.2.0+ reorganization that moves modules around.
langchain==0.1.20
# langchain-community must be the older <0.1 series to satisfy langchain 0.1.20
langchain-community>=0.0.38,<0.1
langchain-openai>=0.1.0
# Use the older text-splitters package range required by langchain 0.1.x
langchain-text-splitters>=0.0.1,<0.1
faiss-cpu>=1.7.4
# Note: 'langchain-chroma' removed because this project uses FAISS (langchain_community.vectorstores.FAISS).
